// Re-export from the existing implementation so both `@/components/catalog/*`
// and `@/components/parts/*` import paths resolve to the same component.
export { CatalogDataNotice } from "@/components/parts/CatalogDataNotice";